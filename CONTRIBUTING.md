# Contributing

## Development

```bash
npm install
npm run check
npm run build
```

## Pull requests

- Keep destination-specific logic in `src/destinations/`.
- Keep generic normalization in `src/core/`.
- Do not commit private access tokens, pixel IDs, test event codes, or customer data.
- Update `README.md` when adding settings or destination behavior.
